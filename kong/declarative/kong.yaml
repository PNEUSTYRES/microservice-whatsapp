_format_version: "3.0"

services:
  - name: api
    url: http://app:3012

    routes:
      - name: index
        paths:
          - /
        strip_path: false
        plugins:
          - name: my-auth
            config:
              auth_url: http://app:3012/api/v1/JWT/validate
